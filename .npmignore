# Build artifacts (generated during install)
build/
*.node
*.dylib
*.so
*.dll

# Submodules (too large for npm - prebuilt binaries are distributed separately)
llama.cpp/
liblloyal/

# CMake build files (not needed in npm package)
CMakeLists.txt
CMakeCache.txt
CMakeFiles/

# Git metadata
.git
.gitmodules
.gitignore

# Development dependencies and configs
package-lock.json
node_modules/
.vscode/
.idea/
*.log
.DS_Store

# Development files
test/
tests/
*.test.js
examples/
docs/

# C++ source files (users get prebuilt binaries, not source)
src/

# Test models (too large for npm)
models/

# CI/CD (runs from git checkout, not needed in published package)
.github/

# Markdown files (except README and CHANGELOG which npm includes by default)
*.md
!README.md
!CHANGELOG.md

# Platform packages (generated during CI, not distributed with source)
packages/darwin-*
packages/linux-*
packages/win32-*
packages/template/

# Temporary files
*.tgz
*.tar.gz
*.tmp
